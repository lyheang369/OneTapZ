PORT=5001
CLIENT_URL=http://localhost:5173
PUBLIC_BASE_URL=http://localhost:5173
VITE_API_URL=/api

# MongoDB
# Use your own host/cluster. Do not commit real credentials.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/onetapz?retryWrites=true&w=majority

# JWT
JWT_SECRET=replace_with_a_long_random_secret

# Telegram Login Widget
# Create a bot with BotFather, set the allowed domain to your deployed domain,
# then use the bot username without the @ in VITE_TELEGRAM_BOT_NAME.
VITE_TELEGRAM_BOT_NAME=onetapz_bot
TELEGRAM_BOT_TOKEN=replace_with_bot_token
